# Unsplash Setup Retired

This document is kept only as a historical note.

The current AI Radar beta does not use the old Unsplash/DALL-E hero-image pipeline described here. There is no active `UNSPLASH_ACCESS_KEY` integration in the live dashboard code path.

## Current status

- No active Unsplash setup is required for the Command dashboard
- No active DALL-E fallback is required for feed rendering
- If this visual system returns later, it should be redesigned against the current product brief rather than restored from this legacy document

## What to use instead

For the current beta, focus setup on:

- `SETUP-OPENAI-KEY.md`
- `AI-RADAR-CANONICAL-BRIEF.md`
- `AI-RADAR-SITE-HANDBOOK.md`

If you decide to bring back article imagery later, treat it as a new scoped feature instead of assuming this retired flow still matches the app.
